<% content_for :page_title, @post.title.gsub('-', '—') %>
<section class="section">
	<div class="container">
		<nav class="level">
			<div class="level-left">
				<p class="level-item">
					<strong>tools</strong>
				</p>
			</div>
			<div class="level-right">
				<p class="level-item">
					<%= link_to "Edit", edit_post_path(@post), class: 'button is-info' %>
				</p>
			</div>
		</nav>
		<div class="content">
			<%= @post.content.gsub('-', '—') %>
		</div>
	</div>
</section>